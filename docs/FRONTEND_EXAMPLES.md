# Frontend Examples - Protected Actions

## Доступ к примерам

После запуска приложения, примеры защищённых действий доступны по следующим URL:

### 🏠 Главная страница
**URL:** `http://localhost:3000/en`

На главной странице вы найдёте:
- ✅ Рабочую кнопку "Create Order" с защитой (CLIENT роль)
- ✅ Секцию "Protected Actions Demo" с ссылками на примеры
- ✅ Информацию о функциональности для разработчиков

### 🔒 Простой пример (Simple Demo)
**URL:** `http://localhost:3000/en/examples/simple-protected`

**Что показывает:**
- Базовый пример использования `useProtectedAction`
- Статус авторизации пользователя
- Кнопка с защитой (CLIENT роль)
- Минимальный код для понимания концепции

**Идеально для:**
- Быстрого тестирования
- Первого знакомства с системой
- Демонстрации клиенту

### 🎯 Полная демонстрация (Full Examples)
**URL:** `http://localhost:3000/en/examples/protected-actions`

**Что показывает:**
- 6 различных паттернов использования
- Готовые компоненты (CreateOrderButton, ApplyToOrderButton, ChatInput)
- Простая защита (только auth)
- Защита по ролям (CLIENT, CONTRACTOR, ADMIN)
- Async API вызовы с loading состоянием
- Множественные защищённые действия
- Лог активности в реальном времени
- Примеры кода

**Идеально для:**
- Изучения всех возможностей
- Выбора подходящего паттерна
- Копирования кода для своих фич

## Сценарии тестирования

### Сценарий 1: Незарегистрированный пользователь
1. Откройте `http://localhost:3000/en`
2. Нажмите на кнопку "Create Order"
3. **Ожидается:** Появится модальное окно с предложением регистрации
4. Нажмите "Create Account"
5. **Ожидается:** Редирект на `/register` с сохранением текущего URL
6. После регистрации → автоматический возврат

### Сценарий 2: Авторизованный CLIENT
1. Войдите как CLIENT
2. Откройте `http://localhost:3000/en/examples/simple-protected`
3. Нажмите "Create Order"
4. **Ожидается:** Действие выполнено без модалки ✅

### Сценарий 3: Авторизованный CONTRACTOR пытается создать заказ
1. Войдите как CONTRACTOR
2. Откройте `http://localhost:3000/en/examples/simple-protected`
3. Нажмите "Create Order (CLIENT only)"
4. **Ожидается:** Ошибка "Недостаточно прав" ❌

### Сценарий 4: Тестирование всех примеров
1. Откройте `http://localhost:3000/en/examples/protected-actions`
2. Протестируйте каждую кнопку
3. Наблюдайте за логом активности справа
4. Смотрите на изменение поведения в зависимости от роли

## Компоненты на главной странице

### 1. CreateOrderButton в Call-to-Action
```tsx
<CreateOrderButton 
  text="Create Order"
  variant="primary"
  className="min-w-[200px]"
/>
```

**Поведение:**
- Если не авторизован → показывает AuthModal
- Если авторизован как CLIENT → редирект на страницу создания заказа
- Если авторизован как CONTRACTOR/ADMIN → показывает ошибку

### 2. Protected Actions Demo секция
Красивая секция с градиентом, содержащая:
- Описание функциональности
- 2 кнопки для перехода к примерам
- Адаптивный дизайн (mobile-friendly)

### 3. Developer Features
Информационные карточки о:
- Protected Actions System
- React Hooks & Components
- Тэги с ключевыми технологиями

## Навигация между примерами

Используйте компонент `ProtectedActionsExamplesNav` для добавления навигации:

```tsx
import { ProtectedActionsExamplesNav } from '@/components/features/examples/protected-actions-nav';

<ProtectedActionsExamplesNav />
```

## Структура файлов

```
frontend/
├── app/
│   └── [locale]/
│       ├── page.tsx                              ← Обновлена (главная)
│       └── examples/
│           ├── simple-protected/
│           │   └── page.tsx                      ← Новая (простой пример)
│           └── protected-actions/
│               └── page.tsx                      ← Новая (полная демо)
│
└── components/
    └── features/
        ├── orders/
        │   ├── create-order-button.tsx           ← Используется на главной
        │   └── apply-to-order-button.tsx
        ├── chat/
        │   └── chat-input.tsx
        └── examples/
            └── protected-actions-nav.tsx         ← Навигация (опционально)
```

## Кастомизация

### Изменить сообщения в модалке

Отредактируйте `frontend/components/auth/auth-modal.tsx`:

```tsx
// Изменить заголовок
<h2>Your Custom Title</h2>

// Изменить описание benefits
<ul>
  <li>Your benefit 1</li>
  <li>Your benefit 2</li>
</ul>
```

### Добавить новый пример

1. Создайте новый файл в `app/[locale]/examples/your-example/page.tsx`
2. Используйте `useProtectedAction` hook
3. Добавьте ссылку на главную страницу
4. Обновите `protected-actions-nav.tsx` (если используете)

### Изменить стили главной страницы

Секция Demo находится в `app/[locale]/page.tsx`:

```tsx
{/* Protected Actions Demo Link */}
<div className="bg-gradient-to-r from-blue-600 to-purple-600 ...">
  {/* Ваша кастомизация */}
</div>
```

## Отладка

### Модалка не появляется
- Проверьте консоль браузера на ошибки
- Убедитесь что `AuthModal` импортирован
- Проверьте `isOpen={showModal}`

### Редирект не работает
- Откройте DevTools → Application → Session Storage
- Проверьте наличие `redirect_after_auth`
- Убедитесь что используете обновлённые формы login/register

### Кнопка не работает на главной
- Убедитесь что `CreateOrderButton` импортирован корректно
- Проверьте что компонент является 'use client' (уже есть)
- Проверьте консоль на ошибки

## Дополнительная информация

- 📖 Документация: `/docs/PROTECTED_ACTIONS.md`
- 🇷🇺 Быстрый старт: `/docs/PROTECTED_ACTIONS_RU.md`
- 💻 Примеры кода: `/docs/examples/protected-actions-examples.tsx`
- 📊 Диаграммы: `/docs/FLOW_DIAGRAM.md`

---

**Статус:** ✅ Готово к тестированию
**Обновлено:** 2 ноября 2025

