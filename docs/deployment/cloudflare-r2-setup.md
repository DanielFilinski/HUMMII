# Cloudflare R2 Setup Guide for Hummii Backend

**Last Updated:** November 3, 2025  
**Для:** Заказчик  
**Приоритет:** 🔴 КРИТИЧНО для Phase 2

---

## 📋 Обзор

Cloudflare R2 - это S3-совместимое хранилище объектов с нулевыми комиссиями за исходящий трафик. Проект использует R2 вместо AWS S3 для:
- Аватары пользователей
- Портфолио контракторов (до 10 фото)
- Лицензии (PDF документы)
- Фото заказов

**Преимущества R2 над AWS S3:**
- ✅ **Нет комиссии за egress** (AWS S3 берет ~$0.09/GB за исходящий трафик)
- ✅ **Встроенный CDN** (автоматическое распространение по всему миру)
- ✅ **S3-compatible API** (используем тот же AWS SDK)
- ✅ **Дешевле:** $0.015/GB хранения (AWS S3: $0.023/GB)
- ✅ **Низкая задержка** благодаря глобальной сети Cloudflare

---

## 🚀 Инструкции по настройке

### Шаг 1: Создание Cloudflare аккаунта

1. Перейди на https://dash.cloudflare.com/sign-up
2. Зарегистрируйся с рабочей email
3. Подтверди email
4. **Не требуется** добавление домена сразу (можно позже)

---

### Шаг 2: Включение R2

1. В dashboard Cloudflare → левое меню → **R2**
2. Нажми **"Purchase R2"** (есть бесплатный tier)
3. Выбери **"Free Plan"**:
   - 10 GB хранения бесплатно/месяц
   - 1 миллион Class A операций (запись)
   - 10 миллионов Class B операций (чтение)
   - **0$ за egress** (исходящий трафик)

**Примечание:** Этого достаточно для MVP на 2-3 месяца. Потом ~$5-10/месяц.

---

### Шаг 3: Создание R2 Bucket

1. В R2 dashboard → **"Create bucket"**
2. Имя bucket: `hummii-production` (или `hummii-dev` для разработки)
3. Location: **Automatic** (Cloudflare автоматически выбирает лучшую локацию)
4. Нажми **"Create bucket"**

---

### Шаг 4: Получение API credentials

1. В R2 dashboard → **"Manage R2 API Tokens"**
2. Нажми **"Create API token"**
3. Настройки token:
   - **Token name:** `hummii-backend-token`
   - **Permissions:** `Object Read & Write`
   - **TTL:** No expiry (или 1 год для безопасности)
   - **Bucket:** `hummii-production` (или выбери specific bucket)
4. Нажми **"Create API Token"**
5. **ВАЖНО:** Скопируй и сохрани:
   - `Access Key ID` (начинается с буквы, ~20 символов)
   - `Secret Access Key` (длинная строка, ~40 символов)
   
   ⚠️ **Secret Access Key показывается только один раз!** Сохрани в безопасное место (1Password, LastPass, etc.)

---

### Шаг 5: Получение R2 Endpoint URL

1. В R2 dashboard → выбери свой bucket (`hummii-production`)
2. Справа найди **"Bucket Details"**
3. Скопируй **S3 API Endpoint**:
   ```
   https://<account_id>.r2.cloudflarestorage.com
   ```
   Пример: `https://abc123def456.r2.cloudflarestorage.com`

---

### Шаг 6: Настройка Public URL (опционально)

Для доступа к файлам через публичный URL:

**Вариант A: Использовать R2.dev subdomain (быстро)**
1. В bucket settings → **"Settings"**
2. Включи **"Public access"** → **"Allow Access"**
3. Cloudflare создаст публичный URL:
   ```
   https://pub-<random>.r2.dev
   ```
4. Скопируй этот URL - это твой `R2_PUBLIC_URL`

**Вариант B: Использовать собственный домен (рекомендуется для production)**
1. В Cloudflare dashboard → добавь свой домен (например, `hummii.ca`)
2. В R2 bucket → **"Settings"** → **"Custom Domains"**
3. Добавь subdomain: `cdn.hummii.ca`
4. Cloudflare автоматически создаст DNS запись
5. Используй `https://cdn.hummii.ca` как `R2_PUBLIC_URL`

---

### Шаг 7: Настройка CORS (для загрузки с frontend)

1. В bucket settings → **"Settings"** → **"CORS Policy"**
2. Добавь CORS rule:

```json
[
  {
    "AllowedOrigins": [
      "https://hummii.ca",
      "https://www.hummii.ca",
      "http://localhost:3001"
    ],
    "AllowedMethods": ["GET", "PUT", "POST", "DELETE"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

3. Нажми **"Save"**

---

## 🔐 Переменные окружения для Backend

Добавь в `/api/.env` файл:

```bash
# Cloudflare R2 Configuration (S3-compatible)
R2_ENDPOINT=https://<твой_account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=<твой_access_key_id>
R2_SECRET_ACCESS_KEY=<твой_secret_access_key>
R2_BUCKET_NAME=hummii-production
R2_PUBLIC_URL=https://pub-<random>.r2.dev
# Или: R2_PUBLIC_URL=https://cdn.hummii.ca (если используешь собственный домен)
```

---

## ✅ Проверка настройки

### Тестирование через AWS CLI (опционально)

1. Установи AWS CLI: `brew install awscli` (macOS) или `apt install awscli` (Linux)
2. Настрой credentials:
   ```bash
   aws configure --profile r2
   ```
   - AWS Access Key ID: `<твой R2_ACCESS_KEY_ID>`
   - AWS Secret Access Key: `<твой R2_SECRET_ACCESS_KEY>`
   - Default region: `auto`
   - Default output: `json`

3. Проверь доступ к bucket:
   ```bash
   aws s3 ls s3://hummii-production --endpoint-url https://<твой_endpoint> --profile r2
   ```

4. Загрузи тестовый файл:
   ```bash
   echo "Test file" > test.txt
   aws s3 cp test.txt s3://hummii-production/test.txt \
     --endpoint-url https://<твой_endpoint> --profile r2
   ```

5. Проверь, что файл доступен:
   ```bash
   curl https://pub-<random>.r2.dev/test.txt
   # Должен вернуть: "Test file"
   ```

---

## 📊 Мониторинг и использование

1. В R2 dashboard → выбери bucket
2. Вкладка **"Metrics"** показывает:
   - Количество объектов
   - Используемое хранилище (GB)
   - Количество операций (Class A/B)
   - **Egress traffic (всегда $0)**

---

## 💰 Оценка стоимости (после Free Tier)

| Метрика | Free Tier | Цена после |
|---------|-----------|------------|
| **Хранение** | 10 GB | $0.015/GB/месяц |
| **Class A ops** (загрузка) | 1M requests | $4.50 за 1M |
| **Class B ops** (чтение) | 10M requests | $0.36 за 1M |
| **Egress** (исходящий трафик) | Без лимита | **$0** (бесплатно!) |

**Пример расчета для 1000 пользователей:**
- 1000 аватаров (1 MB каждый) = 1 GB = $0.015
- 5000 портфолио фото (2 MB каждое) = 10 GB = $0.15
- 500K загрузок/месяц (Class B) = $0.018
- **Итого:** ~$0.18/месяц vs AWS S3: ~$10-15/месяц (с egress)

---

## 🔧 Интеграция с Backend

Код уже готов в:
- `api/src/shared/file-upload/file-upload.service.ts`
- Использует AWS SDK S3 Client (совместимо с R2)
- Автоматическая оптимизация изображений (resize, compress)
- EXIF metadata stripping для приватности

**Никаких изменений в коде не требуется после настройки environment variables!**

---

## ❓ Часто задаваемые вопросы

### 1. Зачем менять AWS S3 на Cloudflare R2?

**Ответ:** Экономия денег. AWS S3 берет комиссию за исходящий трафик (~$0.09/GB), что может обойтись в сотни долларов при росте платформы. Cloudflare R2 - **$0 за egress**.

### 2. Совместим ли R2 с AWS S3 SDK?

**Ответ:** Да, полностью. R2 реализует S3-compatible API. Используем тот же `@aws-sdk/client-s3` без изменений.

### 3. Где физически хранятся файлы?

**Ответ:** Cloudflare автоматически распределяет данные по своей глобальной сети (270+ дата-центров). Файлы реплицируются для надежности.

### 4. Что если Cloudflare упадет?

**Ответ:** Cloudflare имеет 99.99% uptime SLA. В случае проблем можно быстро мигрировать на AWS S3 (S3-compatible API).

### 5. Нужен ли CDN дополнительно?

**Ответ:** Нет. R2 автоматически использует Cloudflare CDN для всех файлов. Файлы кэшируются в ближайших точках присутствия.

### 6. Как безопасно хранить файлы?

**Ответ:**
- R2 автоматически шифрует все данные at rest (AES-256)
- HTTPS для всех операций (encryption in transit)
- Bucket по умолчанию private (доступ только через API)
- Public URL только для изображений (аватары, портфолио)
- Документы (лицензии) - через signed URLs (временные ссылки)

---

## 📞 Поддержка

**Если возникли вопросы:**
1. Cloudflare R2 Documentation: https://developers.cloudflare.com/r2/
2. Cloudflare Support (платный план): https://support.cloudflare.com/
3. Community Forum: https://community.cloudflare.com/

**Для разработчиков:**
- Discord: https://discord.gg/cloudflaredev
- GitHub Issues: https://github.com/cloudflare/cloudflare-docs/issues

---

## 🎯 Следующие шаги

После настройки R2:
1. ✅ Отправь мне environment variables (через 1Password Secure Share или аналог)
2. ✅ Я обновлю backend configuration
3. ✅ Протестируем загрузку файлов
4. ✅ Продолжим Phase 2 implementation (контракторские профили)

---

**Ориентировочное время настройки:** 15-20 минут  
**Статус:** Готово к внедрению  
**Создано:** November 3, 2025

