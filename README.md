# Hummii Design System

Полностью функциональная дизайн-система с набором компонентов UI для современных веб-приложений.

## 🚀 Быстрый старт

```bash
cd frontend
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) для просмотра дизайн-системы.

## 📦 Структура проекта

```
frontend/
├── app/
│   ├── [locale]/
│   │   ├── design-system/    # Страница showcase дизайн-системы
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── globals.css            # Глобальные стили и CSS переменные
│   ├── layout.tsx
│   └── providers.tsx
├── components/
│   └── ui/                    # UI компоненты
│       ├── button/            # Button, PrimaryButton, AuthButton
│       ├── header/            # Header компоненты
│       ├── typography/        # Типографика система
│       ├── Avatar.tsx
│       ├── Badge.tsx
│       ├── Card.tsx
│       ├── Checkbox.tsx
│       ├── Container.tsx
│       ├── Input.tsx
│       ├── Radio.tsx
│       ├── SearchInput.tsx
│       ├── Select.tsx
│       ├── Spinner.tsx
│       ├── Textarea.tsx
│       ├── Toggle.tsx
│       └── index.ts
├── lib/
│   ├── design-tokens.ts       # Токены дизайна (цвета, градиенты)
│   └── utils.ts               # Утилиты (cn, clsx)
└── public/                    # Статические файлы
```

## 🎨 Компоненты

### Основные компоненты
- **Typography** - Система типографики с автоматической адаптивностью
- **Button** - Кнопки различных вариантов (primary, secondary, outline, ghost, danger)
- **Card** - Карточки с различными стилями
- **Input** - Поля ввода с валидацией
- **Textarea** - Текстовые области
- **Select** - Выпадающие списки
- **Checkbox** - Чекбоксы
- **Radio** - Радио кнопки
- **Toggle** - Переключатели
- **Badge** - Бейджи и метки
- **Avatar** - Аватары и группы аватаров
- **Spinner** - Индикаторы загрузки
- **SearchInput** - Поисковая строка
- **Container** - Контейнер с адаптивной шириной
- **Header** - Навигационный header

### Дизайн-токены
Все цвета и градиенты определены в `lib/design-tokens.ts`:
- Цвета фона (background)
- Цвета текста (text)
- Акцентные цвета (accent)
- Цвета обратной связи (feedback)
- Градиенты (gradients)

## 🔧 Технологии

- **Next.js 16** - React фреймворк
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Утилитарные стили
- **class-variance-authority** - Управление вариантами компонентов
- **lucide-react** - Иконки

## 📝 Разработка

### Добавление нового компонента

1. Создайте компонент в `components/ui/`
2. Экспортируйте его в `components/ui/index.ts`
3. Используйте дизайн-токены из `lib/design-tokens.ts`
4. Добавьте пример использования на страницу `app/[locale]/design-system/page.tsx`

### Работа с цветами

Всегда используйте семантические CSS переменные из `globals.css`:

```tsx
// ✅ Правильно
<div className="bg-background-1 text-text-primary">

// ❌ Неправильно
<div className="bg-white text-black">
```

### Типографика

Используйте компонент Typography для всего текста:

```tsx
import { Typography } from '@/components/ui';

<Typography variant="h1">Заголовок</Typography>
<Typography variant="body">Текст</Typography>
```

## 📚 Документация

Подробная документация находится в:
- `frontend/DESIGN_SYSTEM.md` - Обзор дизайн-системы
- `frontend/components/ui/DESIGN_SYSTEM.md` - Документация компонентов
- `frontend/components/ui/typography/TYPOGRAPHY_GUIDE.md` - Руководство по типографике

## 🎯 Примеры использования

Все компоненты с примерами использования можно увидеть на странице:
```
http://localhost:3000/ru/design-system
```

## 📄 Лицензия

MIT
