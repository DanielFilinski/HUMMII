# 📄 Компиляция Figma страниц в HTML

Этот скрипт объединяет извлеченные файлы из Figma (HTML, CSS, JSON) в единую просматриваемую HTML страницу со всеми стилями и метаданными.

---

## 🚀 Быстрый старт

### 1. Извлеките компонент из Figma

```bash
npm run component -- --node-id "144:5"
```

### 2. Скомпилируйте в единую страницу

```bash
npm run compile -- --component=1280-1920 --output=main-page.html
```

### 3. Откройте в браузере

```bash
# Linux
xdg-open output/compiled/main-page.html

# macOS
open output/compiled/main-page.html

# Windows
start output/compiled/main-page.html
```

---

## 📖 Команды

### **Компиляция с параметрами**

```bash
npm run compile -- --component=<component-name> --output=<filename>
```

**Параметры:**
- `--component=<name>` - Имя компонента для компиляции (например: `1280-1920`)
- `--output=<filename>` - Имя выходного файла (по умолчанию: `index.html`)
- `--input=<dir>` - Директория с исходными файлами (по умолчанию: `./output`)

### **Примеры**

```bash
# Скомпилировать главную страницу
npm run compile -- --component=1280-1920 --output=main-page.html

# Скомпилировать компонент без указания имени (возьмет последний)
npm run compile -- --output=latest.html

# Скомпилировать с указанием директории
npm run compile -- --input=./output --component=button --output=button.html
```

---

## 📦 Что делает скрипт?

1. **Находит файлы** - Ищет последние извлеченные HTML, CSS и JSON файлы компонента
2. **Объединяет стили** - Встраивает CSS стили в `<style>` тег
3. **Добавляет Reset CSS** - Нормализует отображение в браузере
4. **Добавляет Info Panel** - Показывает метаданные компонента
5. **Добавляет контейнер** - Оборачивает в красивый контейнер с тенью
6. **Добавляет интерактивность** - JavaScript для логирования и debug mode

---

## 🎨 Результат

Скомпилированная страница включает:

### **1. Info Panel (верхняя панель)**
- Название компонента
- Тип (FRAME, COMPONENT, и т.д.)
- Размеры (ширина × высота)
- Дата генерации

### **2. Контейнер с дизайном**
- Белый фон
- Тень для глубины
- Адаптивная ширина (до 1920px)
- Скругленные углы

### **3. Встроенные стили**
- CSS Reset для нормализации
- Стили компонента из Figma
- Базовые адаптивные стили
- Hover эффекты (в debug mode)

### **4. JavaScript**
- Логирование метаданных в консоль
- Обработка кликов по ссылкам (предпросмотр)
- Debug mode (добавьте `?debug=true` в URL)

---

## 🐛 Debug Mode

Откройте страницу с параметром `?debug=true`:

```
file:///path/to/output/compiled/main-page.html?debug=true
```

**Что делает debug mode:**
- Подсвечивает элементы при наведении (красная рамка)
- Показывает структуру компонента
- Логирует все взаимодействия в консоль

---

## 📂 Структура выходных файлов

```
scripts/figma-extractor/
└── output/
    ├── 1280-1920-*.html           # Исходный HTML
    ├── 1280-1920-*.css            # Исходный CSS
    ├── 1280-1920-*.json           # Метаданные
    └── compiled/
        └── main-page.html         # ✅ Скомпилированная страница
```

---

## 💡 Workflow для дизайна

### **Сценарий 1: Просмотр главной страницы**

```bash
# 1. Извлечь главную страницу (144:5)
npm run component -- --node-id "144:5"

# 2. Скомпилировать
npm run compile -- --component=1280-1920 --output=main-page.html

# 3. Открыть в браузере
xdg-open output/compiled/main-page.html
```

### **Сценарий 2: Сравнение версий**

```bash
# Извлечь компонент
npm run component -- --node-id "123:456"

# Скомпилировать с разными именами
npm run compile -- --component=button --output=button-v1.html
npm run compile -- --component=button --output=button-v2.html

# Открыть обе версии для сравнения
```

### **Сценарий 3: Презентация дизайна**

```bash
# Скомпилировать все ключевые страницы
npm run compile -- --component=1280-1920 --output=home.html
npm run compile -- --component=profile --output=profile.html
npm run compile -- --component=search --output=search.html

# Отправить файлы из output/compiled/ заказчику
```

---

## 🔧 Настройка

### **Изменить базовые стили**

Отредактируйте `compile-page.ts`, функция `generateBaseStyles()`:

```typescript
function generateBaseStyles(): string {
  return `
/* Ваши кастомные стили */
body {
  background: #yourcolor;
}
  `;
}
```

### **Добавить дополнительные скрипты**

Отредактируйте секцию `<script>` в шаблоне HTML:

```typescript
const compiledHtml = `...
  <script>
    // Ваш JavaScript код
    console.log('Custom script loaded');
  </script>
</body>
</html>`;
```

---

## ❓ FAQ

### **Q: Почему компонент не найден?**
**A:** Убедитесь, что вы сначала извлекли компонент командой `npm run component`. Скрипт ищет файлы по имени компонента.

### **Q: Как скомпилировать последний извлеченный компонент?**
**A:** Просто запустите `npm run compile` без параметра `--component`.

### **Q: Можно ли изменить стили скомпилированной страницы?**
**A:** Да, отредактируйте `compile-page.ts` или добавьте свой CSS в функцию `generateBaseStyles()`.

### **Q: Как добавить Google Fonts?**
**A:** Добавьте `<link>` в секцию `<head>` шаблона в `compile-page.ts`.

### **Q: Страница не отображается корректно**
**A:** Убедитесь, что:
1. Компонент был успешно извлечен
2. CSS файл существует
3. Откройте в современном браузере (Chrome, Firefox, Safari)
4. Проверьте консоль браузера на ошибки

---

## 🎯 Расширенные возможности

### **Автоматическая компиляция**

Создайте скрипт для автоматической компиляции после извлечения:

```bash
#!/bin/bash
# extract-and-compile.sh

# Извлечь компонент
npm run component -- --node-id "$1"

# Найти имя компонента из последнего файла
COMPONENT_NAME=$(ls -t output/*.html | head -1 | sed 's/.*output\/\(.*\)-2025.*/\1/')

# Скомпилировать
npm run compile -- --component="$COMPONENT_NAME" --output="$2"

echo "✅ Done! Open: output/compiled/$2"
```

Использование:
```bash
./extract-and-compile.sh "144:5" "main-page.html"
```

---

## 🚀 Готово!

Теперь вы можете:
- ✅ Извлекать компоненты из Figma
- ✅ Компилировать их в единую HTML страницу
- ✅ Просматривать результат в браузере
- ✅ Использовать debug mode для анализа
- ✅ Делиться результатами с командой

**Happy coding! 🎨**





