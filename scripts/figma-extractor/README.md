# 🎨 Figma Extractor - БЕСПЛАТНОЕ извлечение дизайна

> **Полностью бесплатный инструмент для извлечения HTML/CSS, цветов, шрифтов и компонентов из Figma**
> 
> ✅ Не требует платной подписки Figma  
> ✅ Работает с Figma REST API (500 запросов/час)  
> ✅ Извлекает максимум информации из дизайна  

---

## 🚀 Быстрый старт

### 1. Установка

```bash
cd scripts/figma-extractor
npm install
```

### 2. Настройка

Создайте файл `.env` на основе `env.example`:

```bash
cp env.example .env
```

Заполните `.env`:

```env
FIGMA_TOKEN=figd_YOUR_TOKEN_HERE
FIGMA_FILE_KEY=YOUR_FILE_KEY_HERE
```

**📖 Где взять токен и file key?** → Смотрите [QUICKSTART.md](./QUICKSTART.md)

### 3. Запуск

```bash
# Извлечь ВСЁ (структура, компоненты, список)
npm run extract

# Извлечь все цвета
npm run colors

# Извлечь все шрифты
npm run fonts

# Извлечь конкретный компонент
npm run component -- --node-id "123:456"
```

---

## 📦 Что можно извлечь БЕСПЛАТНО?

### ✅ Цвета
- Background colors (HEX, RGB, RGBA)
- Border colors
- Shadow colors
- Gradient stops (все точки градиента)
- **Экспорт:** JSON, CSS variables, SCSS variables, Tailwind config

### ✅ Типографика
- Font family
- Font size (px)
- Font weight (100-900)
- Line height (px, %)
- Letter spacing (px)
- Text align (left, center, right, justify)
- Text transform (uppercase, lowercase, capitalize)
- Text decoration (underline, strikethrough)
- Font style (italic, normal)
- **Экспорт:** JSON, CSS classes, SCSS mixins, Tailwind config

### ✅ Размеры и Layout
- Width / Height
- Padding (top, right, bottom, left)
- Border radius (px)
- Border width и color
- Opacity
- Auto Layout → Flexbox (direction, gap, align, justify)
- **Экспорт:** CSS, Tailwind classes

### ✅ Эффекты
- Drop shadows
- Inner shadows
- Box shadows (offset, blur, spread, color)
- Layer blur (blur radius)
- **Экспорт:** CSS box-shadow, filter

### ✅ Структура
- Полная иерархия компонентов
- Все фреймы и группы
- Вложенные элементы
- **Экспорт:** JSON tree, HTML structure

---

## 📖 Документация

- **[QUICKSTART.md](./QUICKSTART.md)** - Подробная инструкция с примерами
- **[env.example](./env.example)** - Шаблон конфигурации
- **[types.ts](./types.ts)** - TypeScript типы Figma API

---

## 🎯 Примеры использования

### Пример 1: Извлечь Button компонент

```bash
# 1. Найдите Node ID кнопки в Figma
# 2. Запустите:
npm run component -- --node-id "123:456"

# 3. Результат:
#    output/button-2025-11-03.json        - Все данные
#    output/button-2025-11-03.css         - CSS стили
#    output/button-2025-11-03.html        - HTML разметка
#    output/button-2025-11-03-tailwind.html - Tailwind классы
```

### Пример 2: Синхронизировать цвета в Tailwind

```bash
# 1. Извлечь цвета
npm run colors

# 2. Скопировать в проект
cp output/tailwind-colors-*.js ../../tailwind.config.colors.js

# 3. Импортировать в tailwind.config.ts
```

```typescript
// tailwind.config.ts
import colors from './tailwind.config.colors.js';

export default {
  theme: {
    extend: {
      colors: colors.theme.extend.colors,
    },
  },
};
```

### Пример 3: Экспорт всех шрифтов

```bash
# 1. Извлечь шрифты
npm run fonts

# 2. Импортировать CSS классы
cp output/fonts-*.css ../../app/styles/figma-fonts.css
```

---

## 🔧 Команды

| Команда | Описание | Результат |
|---------|----------|-----------|
| `npm run extract` | Извлечь всё из файла | JSON, HTML, tree структура |
| `npm run colors` | Извлечь все цвета | JSON, CSS, SCSS, Tailwind |
| `npm run fonts` | Извлечь все шрифты | JSON, CSS, SCSS, Tailwind |
| `npm run component -- --node-id "X:Y"` | Извлечь компонент | HTML, CSS, Tailwind |
| `npm run compile` | Скомпилировать в единую страницу | Полная HTML страница |
| `npm run preview` | Скомпилировать и открыть в браузере | Автоматический просмотр |

---

## 💰 Стоимость

**$0 / месяц** - Полностью бесплатно!

- ✅ Не требует Figma Professional
- ✅ Не требует Dev Mode
- ✅ Работает с бесплатным Figma аккаунтом
- ✅ Лимит: 500 запросов/час (более чем достаточно)

---

## 🆚 Сравнение с MCP

| Функция | Figma MCP (Платно) | Этот инструмент (Бесплатно) |
|---------|-------------------|---------------------------|
| **Стоимость** | $17/мес | $0 |
| **Цвета** | ✅ | ✅ |
| **Шрифты** | ✅ | ✅ |
| **Размеры** | ✅ | ✅ |
| **Auto Layout** | ✅ | ✅ |
| **Shadows** | ✅ | ✅ |
| **Экспорт CSS** | ✅ | ✅ |
| **Экспорт Tailwind** | ✅ | ✅ |
| **Dev Mode** | ✅ | ❌ |
| **Real-time sync** | ✅ | ⚠️ Polling |
| **AI генерация** | ✅ | ❌ |

---

## ❓ FAQ

### Нужна ли платная версия Figma?
**НЕТ!** Работает с бесплатным аккаунтом. Нужен только Personal Access Token.

### Есть ли лимиты?
**ДА.** 500 запросов в час. Для большинства случаев этого более чем достаточно.

### Можно ли извлечь анимации?
**Частично.** Можно получить transition параметры, но не keyframe анимации.

### Можно ли извлечь изображения?
**ДА!** Используйте Figma Images API (добавьте в скрипт самостоятельно или запросите у меня).

### Работает с Community файлами?
**ДА**, если у вас есть доступ к файлу (даже view-only достаточно).

---

## 🐛 Troubleshooting

### Ошибка: "Invalid token"
```bash
# Проверьте токен в .env
# Убедитесь, что нет лишних пробелов и кавычек
FIGMA_TOKEN=figd_YOUR_TOKEN
```

### Ошибка: "File not found"
```bash
# Проверьте File Key
# Убедитесь, что у вас есть доступ к файлу в Figma
```

### Ошибка: "Node not found"
```bash
# Node ID должен использовать двоеточие, а не дефис
# Правильно: 123:456
# Неправильно: 123-456
```

---

## 📚 Полезные ссылки

- [Figma REST API Docs](https://www.figma.com/developers/api)
- [Как получить Access Token](https://help.figma.com/hc/en-us/articles/8085703771159)
- [Figma File Structure](https://www.figma.com/plugin-docs/api/properties/)

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте [QUICKSTART.md](./QUICKSTART.md)
2. Проверьте FAQ выше
3. Создайте issue с описанием проблемы

---

**Готово! Теперь вы можете извлекать ВСЁ из Figma БЕСПЛАТНО! 🎉**

