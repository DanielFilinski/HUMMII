# Tag Components

Компоненты тегов для отображения статусов задач в проекте Hummii.

## 📁 Структура файлов

```
tags/
├── Tag.tsx              # Базовый компонент тега
├── ClaimedTag.tsx       # Специализированный тег "Claimed"
├── DoneTag.tsx          # Специализированный тег "Done"
├── ReviewedTag.tsx      # Специализированный тег "Reviewed"
├── index.ts             # Экспорты
├── Tag.example.tsx      # Примеры использования
├── Tag.md               # Подробная документация
└── README.md            # Этот файл
```

## 🎨 Дизайн

Компоненты созданы на основе дизайна из файла:
- **Source**: `frontend/src/shared/ui/Tag task state.jpg`

### Варианты тегов

| Тег | Цвет | Иконка | Статус |
|-----|------|--------|--------|
| **ClaimedTag** | Синий (#4A90E2) | `claim` (рука) | Задача взята в работу |
| **DoneTag** | Зелёный (#7ED321) | `order-done` (галочка) | Задача выполнена |
| **ReviewedTag** | Тёмно-зелёный (#5FB547) | `order-reviewed` (звёздочка) | Задача проверена |

## 🚀 Быстрый старт

### Установка

Компоненты уже доступны через barrel export:

```tsx
import { ClaimedTag, DoneTag, ReviewedTag } from '@/src/shared/ui/tags';
// или
import { Tag } from '@/src/shared/ui/tags';
```

### Базовое использование

```tsx
// Специализированные теги (рекомендуется)
<ClaimedTag />
<DoneTag />
<ReviewedTag />

// С кастомным текстом
<ClaimedTag label="В работе" />
<DoneTag label="Выполнено" />
<ReviewedTag label="Проверено" />

// Разные размеры
<ClaimedTag size="sm" />
<DoneTag size="md" />
<ReviewedTag size="lg" />
```

## 📚 Документация

- **Полная документация**: [Tag.md](./Tag.md)
- **Примеры использования**: [Tag.example.tsx](./Tag.example.tsx)
- **Design System**: [frontend/src/screens/design-system/page.tsx](../../screens/design-system/page.tsx)

## 🧩 API

### ClaimedTag / DoneTag / ReviewedTag

```typescript
interface TagProps {
  size?: 'sm' | 'md' | 'lg';     // Размер тега (default: 'md')
  label?: string;                 // Текст тега
  className?: string;             // Дополнительные CSS классы
}
```

### Tag (базовый)

```typescript
interface TagProps {
  variant?: 'claimed' | 'done' | 'reviewed';  // Вариант тега
  size?: 'sm' | 'md' | 'lg';                  // Размер тега
  icon?: IconName;                             // Кастомная иконка
  children: React.ReactNode;                   // Контент тега
  className?: string;                          // Дополнительные CSS классы
}
```

## 📖 Примеры

### В списке задач

```tsx
function TaskList() {
  return (
    <div className="space-y-2">
      <div className="flex items-center justify-between p-3">
        <span>Fix bug</span>
        <ClaimedTag size="sm" />
      </div>
      <div className="flex items-center justify-between p-3">
        <span>Update docs</span>
        <DoneTag size="sm" />
      </div>
    </div>
  );
}
```

### Локализация

```tsx
const translations = {
  en: { claimed: 'Claimed', done: 'Done', reviewed: 'Reviewed' },
  ru: { claimed: 'В работе', done: 'Выполнено', reviewed: 'Проверено' },
};

<ClaimedTag label={translations[locale].claimed} />
```

## 🎯 Когда использовать

- ✅ Отображение статуса задач/заказов
- ✅ Списки задач в таск-менеджерах
- ✅ Карточки заказов
- ✅ Таблицы со статусами
- ✅ Профили подрядчиков (статусы работ)

## ⚡ Производительность

- Компоненты используют `forwardRef` для оптимизации
- CSS transitions вместо JS анимаций
- SVG иконки кэшируются браузером
- Минимальное количество re-renders

## 🔗 Зависимости

- `@/src/shared/ui/icons/Icon` - Компонент иконок
- `@/src/shared/lib/utils` - Утилита `cn()` для классов
- `react` - React библиотека

## 📝 Связанные компоненты

- [Badge](../Badge.tsx) - Для других типов меток
- [Icon](../icons/Icon.tsx) - Иконки в тегах
- [Typography](../typography/Typography.tsx) - Текстовые стили

## 🤝 Contributing

При добавлении новых вариантов тегов:

1. Добавьте новый `variant` в `Tag.tsx`
2. Создайте специализированный компонент (например, `PendingTag.tsx`)
3. Обновите типы в `index.ts`
4. Добавьте примеры в `Tag.example.tsx`
5. Обновите документацию в `Tag.md`

## 📄 Лицензия

MIT License - Hummii Project
