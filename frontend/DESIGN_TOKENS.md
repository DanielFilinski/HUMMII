# 🎨 Design Tokens & CSS Variables

## Архитектура

Этот проект использует **единственный источник истины** для всех дизайн-токенов:

```
src/shared/lib/design-tokens.ts  ← Единственное место для изменений
           ↓
scripts/generate-css-vars.ts     ← Автоматическая генерация
           ↓
app/generated-vars.css            ← CSS-переменные (не редактировать!)
           ↓
app/globals.css                   ← Импорт переменных
           ↓
tailwind.config.ts                ← Использование переменных
```

---

## 🚀 Использование

### 1. Изменение цветов/токенов

**❌ НЕ ДЕЛАЙТЕ ТАК:**
```css
/* globals.css */
:root {
  --color-accent-primary: #3A971E; /* ❌ Не редактируйте здесь! */
}
```

**✅ ДЕЛАЙТЕ ТАК:**
```typescript
// src/shared/lib/design-tokens.ts
export const lightPalette = {
  accent: {
    primary: '#3A971E',  // ✅ Меняйте здесь!
    // ...
  }
}
```

### 2. Генерация CSS

CSS-переменные генерируются **автоматически** при:
- `npm run dev` (predev hook)
- `npm run build` (prebuild hook)

Или вручную:
```bash
npm run generate:css
```

### 3. Добавление новых токенов

**Шаг 1:** Добавьте в `design-tokens.ts`
```typescript
export const lightPalette = {
  // ...
  newCategory: {
    newColor: '#FF0000',
  }
}
```

**Шаг 2:** Запустите генерацию
```bash
npm run generate:css
```

**Шаг 3:** Используйте в компонентах
```tsx
<div className="text-[var(--color-new-category-new-color)]" />
```

Или добавьте в `tailwind.config.ts`:
```typescript
colors: {
  newCategory: {
    newColor: 'var(--color-new-category-new-color)',
  }
}
```

---

## 📦 Файловая структура

### `design-tokens.ts` - Единственный источник истины
Содержит:
- Цветовые палитры (светлая/тёмная)
- Типографику
- Spacing, shadows, transitions
- TypeScript типы

### `generate-css-vars.ts` - Генератор
Автоматически создаёт `generated-vars.css` из токенов TypeScript.

### `generated-vars.css` - Сгенерированный файл
- ⚠️ **НЕ РЕДАКТИРУЙТЕ ВРУЧНУЮ!**
- Автоматически генерируется из `design-tokens.ts`
- Добавлен в `.gitignore` (генерируется на CI/CD)

### `globals.css` - Глобальные стили
- Импортирует `generated-vars.css`
- Содержит базовые стили и утилиты
- НЕ содержит дублирование цветов

### `tailwind.config.ts` - Конфигурация Tailwind
- Использует CSS-переменные из `generated-vars.css`
- Импортирует другие токены из `design-tokens.ts`

---

## 🎯 Преимущества

### ✅ Единственный источник истины
Все токены в одном файле (`design-tokens.ts`)

### ✅ Нет дублирования
CSS-переменные генерируются автоматически

### ✅ Type-safe
TypeScript типы для всех токенов

### ✅ Автоматическая генерация
CSS обновляется при `dev`/`build`

### ✅ Легкое обслуживание
Изменения в одном месте → применяются везде

---

## 🔄 Workflow

### Разработка
```bash
# 1. Измените токены
vim src/shared/lib/design-tokens.ts

# 2. Запустите dev (автоматически генерирует CSS)
npm run dev

# 3. CSS-переменные обновлены автоматически!
```

### Production
```bash
# 1. Build автоматически генерирует CSS
npm run build

# 2. Деплой
```

---

## 📝 Примеры

### Использование в компонентах

```tsx
// ✅ Используйте Tailwind-классы
<div className="bg-background text-text-primary">
  <button className="btn-primary">Click</button>
</div>

// ✅ Или CSS-переменные напрямую
<div style={{ backgroundColor: 'var(--color-background-primary)' }}>
  <p style={{ color: 'var(--color-text-primary)' }}>Text</p>
</div>

// ❌ НЕ используйте прямые цвета
<div style={{ backgroundColor: '#FFFFFF', color: '#2A2A0F' }}>
  ❌ Плохо!
</div>
```

### Добавление нового цвета

**1. Добавьте в токены:**
```typescript
// design-tokens.ts
export const lightPalette = {
  brand: {
    purple: '#8B5CF6',
  }
}

export const darkPalette = {
  brand: {
    purple: '#A78BFA',
  }
}
```

**2. Регенерируйте CSS:**
```bash
npm run generate:css
```

**3. Используйте:**
```tsx
<div className="text-[var(--color-brand-purple)]">
  Фиолетовый текст
</div>
```

---

## 🐛 Troubleshooting

### CSS-переменные не обновляются
```bash
# Остановите dev-сервер
# Запустите генерацию вручную
npm run generate:css

# Перезапустите dev
npm run dev
```

### Переменная не найдена
1. Проверьте, что токен добавлен в `design-tokens.ts`
2. Запустите `npm run generate:css`
3. Проверьте `app/generated-vars.css`

### TypeScript ошибки в токенах
```bash
# Проверьте типы
npm run type-check
```

---

## 📚 Документация

- [Design System Guide](/.github/instructions/design.instructions.md)
- [Typography Guide](/frontend/src/components/ui/TYPOGRAPHY_GUIDE.md)
- [Color Palettes](/frontend/src/shared/lib/design-tokens.ts)

---

## 🔧 Для разработчиков

### Модификация генератора

Если нужно изменить логику генерации CSS:

1. Отредактируйте `scripts/generate-css-vars.ts`
2. Протестируйте: `npm run generate:css`
3. Проверьте результат в `app/generated-vars.css`

### CI/CD Integration

Добавьте в pipeline:
```yaml
- name: Generate CSS Variables
  run: npm run generate:css

- name: Build
  run: npm run build
```

---

## ⚠️ Важные правила

1. **НЕ редактируйте** `generated-vars.css` вручную
2. **Всегда** меняйте токены в `design-tokens.ts`
3. **Запускайте** `generate:css` после изменений токенов
4. **Не коммитьте** `generated-vars.css` (в `.gitignore`)
5. **Используйте** CSS-переменные или Tailwind-классы

---

**Version:** 1.0.0  
**Last Updated:** 2024-12-04
