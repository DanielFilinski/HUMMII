# 🎨 Изменения в Design System

## Что изменилось?

### ✅ Убрано дублирование цветов

**Было:**
- Цвета были определены дважды:
  1. В `design-tokens.ts` (TypeScript)
  2. В `globals.css` (CSS-переменные)
- При изменении нужно было редактировать оба файла
- Высокий риск рассинхронизации

**Стало:**
- Цвета определяются **ТОЛЬКО** в `design-tokens.ts`
- CSS-переменные генерируются **автоматически**
- Изменения в одном месте → применяются везде

---

## 🚀 Как теперь менять цвета?

### 1. Редактируйте только design-tokens.ts

```typescript
// src/shared/lib/design-tokens.ts
export const lightPalette = {
  accent: {
    primary: '#3A971E',  // ← Меняйте здесь
  }
}
```

### 2. CSS генерируется автоматически

```bash
# При запуске dev
npm run dev  # автоматически генерирует CSS

# При build
npm run build  # автоматически генерирует CSS

# Вручную (если нужно)
npm run generate:css
```

### 3. Готово!

CSS-переменные обновлены автоматически и доступны везде:

```tsx
// В компонентах
<div className="bg-accent-primary">Акцентный фон</div>

// Или напрямую
<div style={{ color: 'var(--color-accent-primary)' }}>Текст</div>
```

---

## 📦 Новые файлы

### `scripts/generate-css-vars.ts`
Скрипт генерации CSS-переменных из TypeScript токенов

### `app/generated-vars.css`
Автоматически сгенерированные CSS-переменные  
⚠️ **НЕ редактируйте вручную!**

### `DESIGN_TOKENS.md`
Полная документация по работе с токенами

---

## ⚡ NPM Scripts

```json
{
  "generate:css": "tsx scripts/generate-css-vars.ts",
  "predev": "npm run generate:css",    // ← автоматически перед dev
  "prebuild": "npm run generate:css"   // ← автоматически перед build
}
```

---

## 🎯 Преимущества

✅ **Единый источник истины** - все токены в `design-tokens.ts`  
✅ **Нет дублирования** - CSS генерируется автоматически  
✅ **Type-safe** - TypeScript типы для всех токенов  
✅ **Автоматизация** - CSS обновляется при dev/build  
✅ **Легкое обслуживание** - изменения в одном месте  

---

## 📚 Документация

- **Полное руководство:** [DESIGN_TOKENS.md](./DESIGN_TOKENS.md)
- **Design System:** [.github/instructions/design.instructions.md](../.github/instructions/design.instructions.md)
- **Typography:** [src/components/ui/TYPOGRAPHY_GUIDE.md](./src/components/ui/TYPOGRAPHY_GUIDE.md)

---

**Дата обновления:** 2024-12-04
